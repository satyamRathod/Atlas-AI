# Vectors

A vector is an ordered list of numbers.

In AI, vectors represent semantic meaning.

Key ideas:
- Dimension
- Magnitude
- Dot Product
- Normalization
- Cosine Similarity

RAG uses vectors only for retrieval. The original text is stored alongside the vector and is what gets sent to the LLM.
